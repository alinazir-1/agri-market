// Replaced by shared ScreenFilterChip — kept for backward-compatible imports.
import 'package:flutter/material.dart';
import '../../../../Shared/Screens Common Widgets/screen_filter_chip.dart';

typedef InvFilterChip = ScreenFilterChip;
