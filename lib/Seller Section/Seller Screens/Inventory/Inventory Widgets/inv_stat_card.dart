// Replaced by shared ScreenStatCard — kept for backward-compatible imports.
import 'package:flutter/material.dart';
import '../../../../Shared/Screens Common Widgets/screen_stat_card.dart';

typedef InvStatCard = ScreenStatCard;
