// Replaced by shared ScreenFilterChip — kept for backward-compatible imports.
import 'package:agri_market/shared/widgets/seller/screen_filter_chip.dart';

typedef CustomerFilterChip = ScreenFilterChip;
